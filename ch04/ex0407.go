package main

import (
	"fmt"
)

var desc = `Modify reverse() to reverse the characters of a []byte slice that represents a
UTF-8-encoded string, in place.  Can you do it without allocating new memory?
`

func main() {
	fmt.Println("Ex 4.7")
	fmt.Println(desc)
}
